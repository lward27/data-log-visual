apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "data-log-visual.name" . }}-uploads
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.persistence.storageClass }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
